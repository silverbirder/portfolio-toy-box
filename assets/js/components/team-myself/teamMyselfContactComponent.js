import { html } from "https://unpkg.com/lit-element@2.4.0/lit-element.js?module"
import BaseComponent from "../baseComponent.js";

export default class TeamMyselfContactComponent extends BaseComponent {
    render(){
        return html`
        ${super.preRender()}
        TeamMyselfContactComponent
        `
    }
}
